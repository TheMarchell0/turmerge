export function infoTimeline() {
    /*const infoTimeline = gsap.timeline({
        scrollTrigger: {
            trigger: ".test2",
            start: "top 80%",
            once: true // Выполнить анимацию только один раз
        }
    })
        .to("#box2", { opacity: 1, y: 50, duration: 1 })

        return infoTimeline;*/
}